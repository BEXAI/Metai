// Replaced by the chinese_checkers build track with the real Game implementation.
import { stubGame } from '../../kernel/stub.ts';

export default stubGame("chinese_checkers", "Chinese Checkers", 2, 6);
